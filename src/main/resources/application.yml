spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/art
    username: art
    password: art
---
spring:
  profiles:
    active:
      - production

  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
