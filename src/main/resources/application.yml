spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/art
    username: art
    password: art

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

# Configuration for the development server (Nerd Cave)
discord:
  token: ${DISCORD_TOKEN}
  guild-id: 691227762102501436
  art-channel-id: 895736096548323328 # The channel from which the images are pulled from by the server owner
  repost-channel-id: 895736123878424646 # The channel to which the pictures are posted periodically
  repost-interval: 20 # Number of minutes between each picture reposted
  developer-id: 238728915647070209 # So that the bot can ping me if shit gets wrong

administration:
  password: ${ADMINISTRATION_PASSWORD}
---
spring:
  config:
    activate:
      on-profile: production

  datasource:
    url: jdbc:postgresql://database:5432/art
    name: art
    password: art

  jpa:
    show-sql: false

# Configuration for the production server (Good Gartic)
# TODO: Make this environmental-variables dependent so it can be changed on heroku without changing the code
discord:
  guild-id: ${GUILD_ID}
  art-channel-id: ${ART_CHANNEL_ID}
  repost-channel-id: ${REPOST_CHANNEL_ID}
  repost-interval: ${REPOST_INTERVAL}